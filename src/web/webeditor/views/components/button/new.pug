style
    include style.scss

mixin ButtonNew({ callback, title='Novo Registro', label='Novo Registro' })
    button.form_button.form_button_new(onclick=callback, type='button', title=title)
        span #{label}
            i(class="fa fa-plus")